# 変更履歴

この形式は [Keep a Changelog](https://keepachangelog.com/) に沿う。
版の番号は [Semantic Versioning](https://semver.org/) に従う。

**写して使っている側は、自分が写した版の欄だけを見ればよい。**

| 写している先 | 版 | DOI |
| --- | --- | --- |
| `trinity-infinity` | v0.1.0 | `10.5281/zenodo.22649054` |
| `autonomy-and-self-cultivation` | v0.2.0 | `10.5281/zenodo.22649899` |
| `naval-gazette-notes` | v0.2.0 | `10.5281/zenodo.22649899` |

---

## [0.3.0] — 2026-09-10

DOI: `10.5281/zenodo.22685687`

### 足したもの

- **参考文献の宣言**（`[[reference]]`）。引用した典拠の**どの箇所が、どの主張を
  支えているか**を宣言させる。本文から引かれていない参考文献と、宣言し忘れた
  引用が落ちる。
- `pyproject.toml`。単一モジュールとして梱包し、`errata-check` コマンドを持つ。
  `pypdf` は PDF を一次資料にするときだけの追加依存（`pip install errata-check[pdf]`）。
- `CONTRIBUTING.md`。検査を足すときは壊す試験も足すこと、判定に推論を入れない
  という約束、支えの範囲と後方互換の方針。
- `paper.md` / `paper.bib` / `SUBMIT.md`。JOSS へ出すための一式。

### 直したもの

- `errata_check.py` に MIT の権利表示を入れた。単一ファイルで写して使うため、
  ファイル自身が条件を持っている必要がある。
- **版の番号を上げた。**v0.2.0 のタグから 87 行離れたまま `__version__` が
  `0.2.0` を名乗っていた。**同じ番号が別の中身を指す状態だった。**

### 書いたもの

- AI の利用の開示（README の「AI の利用」）。何を著者が決めたかを含む。

## [0.2.0] — 2026-09-07

DOI: `10.5281/zenodo.22649899`

### 足したもの

**分野ごとの検査**（自身の検査は 16 → 60 項目）。紙面から決定的に確かめられる
ものは分野によって違うため、引用の一致だけを見る形をやめた。

- `[[statistic]]` —— 印字された検定統計量と自由度から `p` を計算し直す
  （`t` / `F` / `χ²` / `r`）。
- `[[grim]]` —— 整数を平均した値が、その標本数で到達できる値か。
- `[[identifier]]` —— ORCID・ISBN・ISSN の検査数字。
- `[[arithmetic]]` —— 印字された数どうしの関係（無作為化 = 解析 + 脱落 など）。
- `[[era]]` —— 和暦と西暦の対応。

## [0.1.0] — 2026-09-07

DOI: `10.5281/zenodo.22649054`

最初の版。**凍結された公開物に対して、正誤表のほうを機械で監査する。**

- `[[quote]]` —— 正誤表が「印字されている」と述べた文が一次資料にあるか。
  `present = false` で、無いことも確かめられる。
- `[[count]]` —— 宣言した件数だけ記述が挙がっているか。
- `[[absent]]` —— 「同梱されている」と謳われたファイルが本当に無いか。
- `[[number]]` —— 散文が名乗る数が、実際にコマンドを走らせた結果と一致するか。
- 未解決の項目が、こっそり解決済みに書き換わっていないか。
- `sha256` —— 一次資料そのものが差し替わっていないか。
- 最終更新が、本文に書かれたどの日付よりも古くないか。

判定に推論を使わない。大規模言語モデルも類似度も用いない。

[0.3.0]: https://github.com/cpsbvbng26-dotcom/errata-check/releases/tag/v0.3.0
[0.2.0]: https://github.com/cpsbvbng26-dotcom/errata-check/releases/tag/v0.2.0
[0.1.0]: https://github.com/cpsbvbng26-dotcom/errata-check/releases/tag/v0.1.0
