# errata-check

凍結された公開物に対して、正誤表のほうを決定的に監査する道具。単一ファイル。
DOI が付いている（v0.2.0 = 10.5281/zenodo.22649899、v0.1.0 = 10.5281/zenodo.22649054）。

<!-- 共通ここから -->
<!-- この区間は 9 つのリポジトリで一字一句同じである。
     cpsbvbng26-dotcom/verification/check_ecosystem.js が一致を見ている。
     一つだけ直すと落ちる。直すときは全部直す。 -->

## 決めごと

**破ると公開物が壊れる順に並べてある。上から効く。**

1. **DOI を推測で埋めない。**利用者から受け取った番号だけを書く。作業環境からは
   `zenodo.org` も `doi.org` も DataCite も見えない（届くのは GitHub だけ）。
   確かめていないなら「確かめていない」と書く。番号を当てにいかない。
2. **公開済みの論文 PDF を改変しない・再生成しない。**凍結された成果物である。
   誤りが見つかったら PDF ではなく正誤表のほうを直し、直せないものは
   `self-correction` に記録する。
3. **生成されたファイルを手で編集しない。**もとの設定を直して生成し直す。
   どのファイルが生成物かは、このあとの「このリポジトリ」に書いてある。
4. **検証スクリプトの期待値・乱数種を、通すために変えない。**落ちたら中身を直す。
   検査を緩めて通すのは、検査を消すのと同じである。
5. **散文に数を書いたら、その数を機械で確かめられるようにする。**
   「NN 項目」「NN 件」「NN 通り」を書き足したら、実際の数と突き合わせる検査も足す。
   ここが、このリポジトリ群でいちばん頻繁にずれる。
6. **自称の肩書きを書かない。**使わないと決めた語は各リポジトリの `check_text`
   にある。凍結した紙面に印字されている分はそのまま（改変しない）。
7. **ライセンスを勝手に変えない。**散文と論文は CC BY 4.0、実装は MIT。
8. **AI 支援の開示は消さない。Claude を著者にしない。**
9. **秘密情報・個人の連絡先を新規に公開しない。**
10. **本文に書かないもの** —— 家系、コンサルタントの肩書き、ライブ配信、
    シーランド公国の称号、在学中の学校名。
11. **JACAR の原資料画像を追加しない。**防衛省防衛研究所 戦史研究センターへの
    連絡が先である。
12. **Zenodo での公開・新しい版の作成は利用者が行う。**こちらは番号を受け取って
    書くだけ。GitHub の Release を勝手に作らない。
13. **コミットは小さく、ファイルの種別ごとに分ける。**生成物と設定は別のコミットにする。

## 環境について

- `gh` は使えない。GitHub の操作は `mcp__github__*` を使う。
- **タグの push は 403 で通らない。**Release の作成は利用者の手作業になる。
- 外に出られるのは GitHub だけ。他のホストは egress proxy が塞いでいる。

## 書き方

記録は事実を並べる文書である。散文には次の四つを当てる。

1. 強調をやめる。太字は、機械が固定している語と数にだけ使う。多用は、
   地の文が弱いという告白になる。
2. である調で書く。ですます調は読み手への配慮だが、配慮は距離を作る。
   記録に距離は要らない。
3. 二人称を使わない。「〜してください」は指導の語である。記録に指導は要らない。
   何があったかだけを書く。
4. 一文に一つだけ置く。修飾を後ろに垂らさない。長い文は、書き手がまだ
   決めていない証拠である。

厳しさは音量ではなく、削った量で出る。

この規則が掛かるのは記録と散文である ―― ERRATA.md、REGISTER.md、SHELF.md、
各 README の地の文。この CLAUDE.md 自身と、コード中の注釈は対象外とする。
どちらも指示書であり、記録ではない。

利用者への返答も対象外である。そこは会話であって、記録ではない。

<!-- 共通ここまで -->

## このリポジトリ

```
python3 tests/check_tool.py                        # 道具自身 63 項目
python3 errata_check.py examples/minimal/audit.toml       # 19 項目
python3 errata_check.py examples/disciplines/audit.toml   # 31 項目
python3 examples/minimal/build.py && python3 examples/disciplines/build.py
git diff --exit-code -- examples                   # 見本が作り直せる
```

`examples/*/paper.pdf` と `record.md` は `build.py` の生成物である。手で編集しない。

**判定に推論を使わない。**LLM も類似度も入れない。一致するかしないかだけで決める。
ここが他の道具との違いなので、崩さない。

`scipy` を入れない。単一ファイルで配れなくなる。

道具自身の検査は、通る状態を壊して落ちることまで見る形で書く。
**壊す先を足したら、`README.md`・`CITATION.cff`・`.zenodo.json` が名乗る数も直す**
（`tests/check_tool.py` が突き合わせている）。
